spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:
  batch:
    jdbc:
      initialize-schema: always

chargeback:
  output:
    directory: ./target/test-output

logging:
  level:
    org.springframework.batch: DEBUG
    com.chargebacks.processor: DEBUG

